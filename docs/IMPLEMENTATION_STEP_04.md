# Крок 04 — виміряний локальний пілот

Дата: 2026-09-07 (Budapest). Протокол/виконавець до запиту: `5d93cd8`.
Пакет: `.benchmarks/local-pilot-step04-001/`.
Anchor: [audit_anchors/step04-001.json](audit_anchors/step04-001.json).

Один запит qwen3.5:27b, перші 8000 символів body «Машини часу».
Збережено 8210 байтів покриття із 181007 (4.536% цього твору), 1 done,
22 pending. Це окремий профіль пілота; у загальному плані 1015 вікон
unassigned-model виконання не приписувалося заднім числом.

Wall time: 38.141 с. Сервер: total 37.988 с, load 33.081 с,
prompt eval 2.973 с, generation 1.731 с. Input 2091 токен, output 27.
Generation throughput 15.60 токена/с. Приблизно 87.1% серверного часу —
завантаження моделі. Висновок: пакет із резидентною моделлю має усунути
повторне завантаження; точну швидкість пакета одним вимірюванням не доведено.

Кандидати: Time Traveller, Fourth Dimension, Time-Dimension,
geometry of Four Dimensions, machine. Усі знайдені у переданому фрагменті.
Це не п'ять нових науково підтверджених термінів: machine є загальним
словом, а пов'язані вислови про виміри можуть дублювати поняття.
Основна база засвідчень залишається на 84 записах. Пілот виміряв
екстракцію, не новизну та лінгвістичну якість енциклопедичних статей.

Exometric та незалежний Manifold перерахунок: PASS цілісності, 23 артефакти.
Повторна перевірка після критичного зауваження також PASS. Незалежний
звіт знаходиться у seal/independent_recompute.json; imports_exometric=false.
Поле pending у журналі bridge описує проміжний стан, не фінальний результат.

Після generation keep_alive=0 модель ще спостерігалася у негайному
postflight, але наступний read-only /api/ps повернув models=[]. Тому
не стверджуємо синхронне миттєве вивантаження. Енергію не вимірювали.
27 тестів пройшли до прогону. Помилок або повторів generation не було.

Наступний дизайн — [протокол класу пакетів](BATCH_AND_EDITORIAL_PROTOCOL_V1.md),
а не нова пререєстрація кожного фрагмента.
